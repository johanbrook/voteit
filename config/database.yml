# Postgre SQL

development:
  adapter: postgresql
  encoding: utf8
  database: voteit_development
  pool: 5
  user: voteit
  password: voteit

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  database: voteit_development
  pool: 5
  user: voteit
  password: voteit

production:
  adapter: postgresql
  encoding: utf8
  database: voteit_production
  pool: 5
  user: voteit
  password:
