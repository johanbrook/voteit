<h2>Votings for <%= @meeting.period%> meeting</h2>

<%= render :template => "votings/index" %>

<h3>New voting</h3>

<div class="admin">
<p>
	<%= link_to 'New Voting', new_meeting_voting_path(@meeting) %> | 
	<%= link_to 'Edit', edit_meeting_path(@meeting) %>
</p>
</div>

<%= link_to '← All meetings', meetings_path %>
